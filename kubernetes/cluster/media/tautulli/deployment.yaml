---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: tautulli
  namespace: flux-system
spec:
  dependsOn:
    - name: dolos-cluster
    - name: downloads-storagevolumes
  path: ./kubernetes/cluster/media/tautulli/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: dolos
  wait: true
  interval: 3m
  timeout: 30m
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-settings
      - kind: Secret
        name: cluster-secrets